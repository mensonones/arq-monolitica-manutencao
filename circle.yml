machine:
  java:
    version: oraclejdk8

test:
  override:
    - mvn integration-test   

general:

  artifacts:
        - "target/monolitica.war"
        - "target/checkstyle-result.xml"

  branches:
      only:
        - master
        - /developer-.*/
