machine:
  java:
    version: oraclejdk8

test:
  override:
    - mvn integration-test   

general:

  artifacts:
        - "target/monolitica.war"

  branches:
      only:
        - master
        - /developer-.*/
