machine:
  java:
    version: oraclejdk8

test:
  override:
    - mvn integration-test
    - cp -R build/reports/tests/* $CIRCLE_ARTIFACTS    

general:

  artifacts:
        - "target/monolitica.war"

  branches:
    only:
      - master
      - developer
